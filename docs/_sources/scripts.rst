.. currentmodule:: tamoc

.. _scripts:

###############
Example Scripts
###############

The `./bin` directory distributed with tamoc contains many examples of using the different modules within the tamoc suite.  Here, we collect each of these examples using the auto-documentation methods of the Sphinx documentation package.  These pages report the header information for each file.  To see the example code, please view the source files in the ``./bin/`` directory distributed with ``tamoc``.

.. toctree::
   :maxdepth: 2
   :hidden:
   
   scripts/ambient
   scripts/dbm
   scripts/single_bubble_model
   scripts/stratified_plume_model
   scripts/bent_plume_model
   scripts/particle_size_models
   scripts/blowout
   scripts/params
   
   